query PokemonDetailScreen($name: String!) {
    pokemon(name: $name) {
        id
        sprites {
            front_default
        }
        types {
            type {
                name
            }
        }
        species {
            name
        }
        abilities {
            ability {
                name
            }
        }
    }
}
